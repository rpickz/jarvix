# Jarvix configuration — hand-tuned, do not lose these notes.

[ai]
provider = "ollama"   # local first
model = "qwen2.5:7b"

# my private endpoint
[ai.myserver]
base_url = "http://10.0.0.5:8080/v1"
api_key_env = "MYSERVER_KEY"

[tts]
provider = "kokoro"

[tts.piper]
voice = "en_US-amy-medium" # robotic but instant
