# my config, hand-written
[intents]
terminal = "alacritty"

# lock it when I walk away
[[intents.custom]]
match = "lock the screen"
run = "hyprlock --immediate"
say = "Locking now."

[[intents.custom]]
match = "mute the music"
run = "playerctl pause"
