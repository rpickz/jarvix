# my config, hand-written
[intents]
terminal = "alacritty"

# lock it when I walk away
[[intents.custom]]
match = "lock the screen"
run = "hyprlock"
say = "Locking."

[[routines]]
name = "evening"
phrases = ["evening mode"]
